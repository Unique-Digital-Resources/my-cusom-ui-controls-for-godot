class_name LucideExporter
extends TextureExporter

# Exporter for Lucide icons.
# The ResourceResolver now safely rasterizes LucideTexture via SubViewport,
# so we can rely entirely on the standard TextureExporter logic.
